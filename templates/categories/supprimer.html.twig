{% extends 'base.html.twig' %}

{% block title %}ajouter une categorie{% endblock %}

{% block body %}
    <h1>Supprimer la catégorie {{ categorie.titre }}</h1>
    {{ form(formulaire) }}
    <a href="{{ path('app_categories') }}" class="btn btn-primary">retour</a>
{% endblock %}
