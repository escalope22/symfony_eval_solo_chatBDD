{% extends 'base.html.twig' %}

{% block title %}Supprimer un chaton{% endblock %}

{% block body %}
    <h1>Supprimer le chaton {{ chat.nom }}</h1>
    {{ form(formulaire) }}
    <a href="{{ path('app_categories') }}" class="btn btn-primary">retour</a>
{% endblock %}
