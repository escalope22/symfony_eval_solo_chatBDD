<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
   aria-expanded="false">Catégories</a>
<ul class="dropdown-menu">
    {% for d in dossiers %}
        <a class="dropdown-item" href="{{ path('app_chatons_voir', {"id" : d.id}) }}">{{ d.Titre }}</a>
    {% endfor %}
</ul>