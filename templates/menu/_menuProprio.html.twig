<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
   aria-expanded="false">Propriétaires</a>
<ul class="dropdown-menu">
    {% for p in proprio %}
        <a class="dropdown-item" href="{{ path('app_lié', {"id" : p.id}) }}">{{ p.prenom }} {{ p.nom }}</a>
    {% endfor %}
</ul>